Beyond SMP Applications V1 — frontend foundation.

The application flow is fully client-side for now. It deliberately does NOT
store or submit application data. The final screen clearly states preview mode.

Supabase can be used later as the database/backend, but any privileged Supabase
key must remain server-side and must never be placed in this public GitHub
Pages frontend.

Replace logo.png with the existing Beyond SMP logo before deploying.
